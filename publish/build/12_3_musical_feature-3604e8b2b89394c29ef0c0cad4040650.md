# Musical Features

Chroma based features are closely connected to the 12 different pitch classes in music {C, C♯, D, D♯, E ,F, F♯, G, G♯, A, A♯, B}. Each pitch class, like C, represents all the possible pitches at all octaves. All pitches are related to each other by octave. If two pitches of the same class are an octave apart, their frequency has a ratio of 1:2 (or 2:1), meaning that with each higher octave, the frequency doubles. Another important concept in music theory is the partials, overtone, fundamental, and harmonics.

  - Every musical instrument creates a mix of sine waves, known as partials. These partials have their own frequencies and vary in amplitude over time and thus giving each instrument its unique sound pattern, or timbre. Our ears are highly skilled at identifying and differentiating between different timbres, allowing us to distinguish between various instruments and voices in audio recordings.

  - The fundamental is the partial with the lowest frequency and corresponds to the pitch we hear. Harmonics are frequencies that are multiples of the fundamental frequency. Instruments may have both harmonic and inharmonic partials, but they are usually designed so that all partials are close to harmonic frequencies.

  - Overtone refers to all the partials except for the fundamental. The relative strength of the overtones determines the unique sound of an instrument as the sound changes over time.

  - The standard pitch A440 (also known as A4 of Stuttgart pitch) sets the A note of middle C at $f_{A4}=440$ Hz and is used as a tuning standard for musical instruments. If we label the pitch classes (also called semitones) with $n=0$ (C), …, $n=11$ (B), we can express the frequency of the semitones in the octave $o$ with $−1\leq o\leq 9$ (MIDI numbers are $12\left(o+1\right)+n$; A4 has number 69).

$f_{A440}\left(o,n\right)=f_{A4}∙2^{\frac{12o+n−57}{12}}=440∙2^{\frac{12o+n−57}{12}}$

Table of note frequencies (standard piano key frequencies)

Source: https://en.wikipedia.org/wiki/Scientific_pitch_notation

To find the pitch in audio files, we need to extract the fundamentals. One way to do this is to match all frequencies from the STFT to a chroma value that represents the pitch class. We use the A440 standard with a frequency of $f_{ref}=440$. Let's $\omega \left(k\right)=k∙f_{s}/N$ be the frequency mapping of the $k$-th Fourier coefficient with a sampling rate of $f_{s}$ and with $N$ samples. Then, we can find the chroma value, that is pitch class $p(k)$ and octave $o(k)$, as follows:

  - To create a chroma histogram, we add up the power spectrum using the mentioned mappings to determine the pitch class and octave. Then, we generate a histogram vector for each frame.

  - Yet, this method does not allow us to capture the main pitch contour (or multiple pitches if the music is polyphonic). Instead, it only gives us a mapping to chroma values. We can estimate the fundamental frequency $f_{0}$ in a specific time window by finding the frequency that maximizes the sum of magnitudes across all its harmonics.

    - $g(k,m)$ is based on evidence and is used to determine the importance of each harmonic. The number $M$ depends on the highest frequency in the spectrum and represents the number of harmonics being considered.

$p\left(k\right)=\left⌊9.5+12log_{2}\left(\frac{k∙f_{s}}{N∙f_{ref}}\right)\right⌋ mod 12$

$o\left(k\right)=\left⌊\frac{1}{12}\left(9.5+12log_{2}\left(\frac{k∙f_{s}}{N∙f_{ref}}\right)\right)\right⌋ $

$h_{chroma}\left(i,o,p\right)=\frac{1}{\sum_{k=0}^{K}\left|X\left(i,k\right)\right|^{2}}∙\sum_{k=0}^{K}\left\{\begin{matrix}\left|X\left(i,k\right)\right|^{2}&if o=o\left(k\right)∧p=p(k)\\0&otherwise                        \end{matrix}\right.$

[MATH_ERROR]

$g\left(k,m\right)=\frac{\omega (k)+27}{\omega (km)+320} =\frac{k\frac{f_{s}}{N}+27}{km\frac{f_{s}}{N}+320}$

  - With the fundamental $f_{0}$, we obtain the pitch class $p(f_{0})$ and the octave $o(f_{0})$ of the time window. To get multiple fundamentals from the frame, we repeat these steps:

    - Compute the magnitude spectrum $\left|X^{(0)}(i,k)\right|$

    - Iterate $t=0, 1, …$ as long as  $\sum_{k=0}^{K}\left|X^{(t)}(i,k)\right|>\varepsilon $

      - Compute $f_{0}$ on the magnitude spectrum $\left|X^{(t)}(i,k)\right|$

      - Modify the magnitude spectrum by subtracting the magnitudes of the harmonics from the computed fundamental $f_{0}$ to get the values for the next iteration $\left|X^{(t+1)}(i,k)\right|$

We can alternatively calculate the fundamental frequency $f_{0}$ in the time domain. To do this, we calculate the autocorrelation of the audio signal at different time shifts $∆t$. Let $N$ be the size of a frame and $f_{s}$ be the sampling rate. To narrow down the search, we set the condition $1/f_{min}\geq ∆t\geq 1/f_{max}$ for the minimum and maximum frequency range for the fundamental $f_{0}$: $f_{min}\leq f_{0}\leq f_{max}$. Additionally, time shifts are whole number multiples of the sampling period: $∆t=m/f_{s}$ with $f_{s}/f_{min}\geq m\geq f_{s}/f_{max}$. The autocorrelation for the frame $F_{i}$ and the lag $m$ is then defined as follows:

  - To find the basic element, we look for the lag $m_{0}$ that gives us the highest autocorrelation and then calculate the frequency from this lag.

$R\left(i,m\right)=\sum_{t=m}^{N}x\left(i,t\right)∙x(i,t−m)$

$f_{0}=\frac{f_{s}}{m_{0}}$

[MATH_ERROR]

Another important aspect of music is the tempo, which is measured in beats per minute (bpm). In classical music, different tempos are defined by ranges such as Largo (40-60 bpm), Larghetto (60-66 bpm), Adagio (66-76 bpm), Andante (76-108 bpm), Moderato (108-120 bpm), Allegro (120-168 bpm), Presto (168-200 bpm), and Prestissimo (200+ bpm). These tempos can change throughout a piece. In pop music, the tempo is usually more consistent, with bpm ranging from 60 to 160, and 120 bpm being the most common choice.

  - Beat tracking is the process of finding the regular bursts of energy at the beat intervals. For a song with a tempo of 100 beats per minute, we would expect to see a spike in energy every 10 milliseconds (give or take, depending on how precise the musician is) to indicate the beats. However, it's not always as simple as that, as the example below demonstrates.

Source: D. Ellis, Beat Tracking by Dynamic Programming, 2007.

Vertical lines denote the beat as found by the method of D. Ellis.

Onsets often at beat intervals, but also in between and with varying intensities
  - Calculation of onset envelope: The onset is the positive slope on the energy over the spectrum at a specific time. Using STFT and mel bands, we get the mel spectrum $|X_{mel}\left(i,b\right)|$ as a weighted function from the frequency spectrum. The weighting ensures that the areas under the triangular mel bands are equal. This enhances the resolution of lower frequencies and emphasizes them over higher frequencies (essentially weighting by the inverse of the band width). The onset is similar to the spectral flux, but only positive slopes (onsets) are considered and then converted to decibels. If $B$ is the number of mel bands and  is the current frame $F_{i}$, then the onset $o\left(i\right)$ is as follows:

  - We estimate the global tempo by analyzing the onset $o(i)$ using a window function $w(i)$, like the Hann function. Basically, we're trying to find a time delay $∆t$ where the peaks in the onset function line up. The time delay that gives us the highest autocorrelation is the global tempo. We can calculate the tempo for each frame to create a tempogram, which shows autocorrelations for each frame $F_{i}$, the lag $l$, and the window function $w(i)$.

  - The tempo is determined by the lag $l_{0}$ with the strongest correlation, and we can convert it to beats per minute with $∆t=l_{0}/f_{s}$ and so the tempo is $\frac{60}{∆t}=60\frac{f_{s}}{l_{0}}$ bpm. Sometimes, we also see other peaks at $\{0.33l_{0},  0.5l_{0},  2l_{0},  3l_{0}\}$ which indicate secondary tempos if their correlation is high enough. Additionally, we may prefer beats around 120 bpm if we know the genre (for example, pop).

  - See visualization on the next page.

$𝑜 𝑖 = 𝑏 =1 𝐵 max 0, log 10 | 𝑋 𝑚𝑒𝑙 𝑖 , 𝑏 | log 10 | 𝑋 𝑚𝑒𝑙 𝑖 −1, 𝑏 | −1$

$a\left(i,l\right)=\sum_{j=0}^{W}w\left(j\right)∙o\left(i+j\right)∙o(i+j+l)$

    - Example for tempo estimation within a time frame:

  - Beat tracking is the process of identifying the time points $\{t_{i}\}$ when the onsets occur in the audio signal, similar to how a person would tap along to the beat. The goal is to find these time points so that they match the tempo of the music, with some small variations allowed. This is done by optimizing an objective function $F(t_{i}−t_{i−1},l_{0})$, which includes a penalty for deviations from the ideal tempo and a weighting to balance the importance of the onset values and penalty values.

Weighting function prefers beats around 120bpm which is a lag of 0.5 seconds. Here the primary tempo is about 168, and the secondary at 84.

$C\left(\left\{t_{i}\right\}\right)=\max_{\{t_{i}\}}\left(\sum_{i=1}^{T}o(f_{s}∙t_{i})+\alpha \sum_{i=2}^{T}F(f_{s}∙(t_{i}−t_{i−1}),l_{0})\right)$

$𝐹 ∆ 𝑙 , 𝑙 0 =− log ∆ 𝑙 𝑙 0 2$
